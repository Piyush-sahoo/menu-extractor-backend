# Server Configuration
PROJECT_NAME="Menu Extractor API"
PORT=8000

# Database (Redis)
# If using Docker Compose, this hostname 'redis' works.
# If running locally without Docker networking, use 'localhost'.
REDIS_URL=redis://localhost:6379/0

# AI/LLM Configuration (Required)
# Get this from https://aistudio.google.com/app/apikey
GEMINI_API_KEY=

# Optional: Google Cloud Vision (if you enable that later)
# GOOGLE_APPLICATION_CREDENTIALS=path/to/key.json
GOOGLE_CREDENTIALS_JSON='{...paste content of key.json here...}'
